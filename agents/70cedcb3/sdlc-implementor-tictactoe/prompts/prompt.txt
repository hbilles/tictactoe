Implement the requested ADW plan.

Execution rules:
- Read the plan carefully and implement the requested work.
- Make the needed code and test changes directly in the working tree.
- Follow existing code patterns and keep the change scoped to the plan.
- Do not create git commits or pull requests.
- If targeted tests or checks are obvious and inexpensive, you may run them.

Plan content:

Implement the following section for repo "tictactoe":

### Relevant Files
- `tictactoe/package.json` — replace Vue-specific runtime and tooling with the React equivalents while preserving the existing Vite/Vitest/Tailwind workflow and script names where possible.
- `tictactoe/vite.config.js` — switch from the Vue Vite plugin to the React plugin and keep the existing dev-server/test behavior intact.
- `tictactoe/eslint.config.js` — remove Vue parsing/rules and replace them with a React-oriented flat config scoped to the new JSX files.
- `tictactoe/src/main.js` — current Vue mount point; this should become the React entry and likely move to `main.jsx`.
- `tictactoe/src/App.vue` — main gameplay UI and state orchestration; this is the largest Vue component that needs a React rewrite.
- `tictactoe/src/game.js` — pure gameplay logic already extracted from the UI; preserve this module as the behavioral contract for the React rewrite.
- `tictactoe/src/assets/main.css` — shared Tailwind/component-layer styling that should continue to drive the React UI.
- `tictactoe/src/components/CellSymbol.vue` — Vue-only rendering wrapper for board symbols; replace with a React component while keeping the same glyph and color behavior.
- `tictactoe/src/components/Icons.vue` — SVG sprite definitions currently rendered through Vue; replace with a React component or static sprite include without changing symbol ids.
- `tictactoe/src/App.test.js` — current Vue Test Utils coverage for click-to-play and reset behavior; rewrite around the React app.
- `tictactoe/src/components/CellSymbol.test.js` — current Vue component assertions for symbol rendering; rewrite for the React component.
- `tictactoe/src/game.test.js` — targeted logic regression suite that should remain in place and continue to validate gameplay rules independently of the framework swap.
- `tictactoe/README.md` — update framework references, local-development instructions, and testing notes to match the React implementation.
- `.claude/commands/e2e/README.md` — reference format for the new UI scenario file that this chore must add.

#### New Files
- `tictactoe/src/main.jsx` — React entrypoint replacing the Vue app bootstrap.
- `tictactoe/src/App.jsx` — React version of the gameplay shell.
- `tictactoe/src/components/CellSymbol.jsx` — React rendering for X/O board cells.
- `tictactoe/src/components/Icons.jsx` — React-hosted SVG symbol definitions used by `CellSymbol`.
- `tictactoe/src/App.test.jsx` — React Testing Library or equivalent component-level gameplay coverage.
- `tictactoe/src/components/CellSymbol.test.jsx` — React component rendering coverage for symbol selection and styling.
- `.claude/commands/e2e/test_tictactoe_react_gameplay.md` — focused UI scenario covering a playable round and reset flow after the React migration.

### Step by Step Tasks
IMPORTANT: Execute every step in order, top to bottom.

#### 1. Replace framework dependencies and config
- Update `tictactoe/package.json` to remove Vue runtime/test/lint packages and add `react`, `react-dom`, `@vitejs/plugin-react`, and the React-compatible test utilities needed for the existing Vitest/jsdom setup.
- Update `tictactoe/vite.config.js` to use the React Vite plugin while preserving the current `server.port` and `test` settings.
- Update `tictactoe/eslint.config.js` so the repo lints the new `*.jsx` files instead of Vue single-file components.

#### 2. Migrate the app entry and gameplay shell
- Replace `tictactoe/src/main.js` with a React entrypoint in `tictactoe/src/main.jsx` that mounts the app into `#app` and continues to import `src/assets/main.css`.
- Rewrite `tictactoe/src/App.vue` as `tictactoe/src/App.jsx`, carrying over the existing `startingPlayer`, `turn`, `gameState`, `strategyIndex`, modal messaging, reset flow, and computer-turn behavior.
- Keep `tictactoe/src/game.js` as the source of truth for outcome detection and AI move selection; only make minimal API adjustments if React integration truly requires them.

#### 3. Port the Vue-only presentation components
- Replace `tictactoe/src/components/CellSymbol.vue` with `CellSymbol.jsx`, preserving the current symbol mapping, inline color behavior, and SVG usage.
- Replace `tictactoe/src/components/Icons.vue` with `Icons.jsx` or an equivalent static React-hosted sprite so `#icon--circle` and `#icon--x` still resolve correctly.
- Remove the obsolete Vue component files once their React replacements are wired in.

#### 4. Rewrite the UI-facing tests around React
- Keep `tictactoe/src/game.test.js` in place as the framework-independent gameplay regression suite.
- Rewrite `tictactoe/src/App.test.js` as `App.test.jsx` to cover the existing user-visible behaviors: human move, computer response, and reset flow.
- Rewrite `tictactoe/src/components/CellSymbol.test.js` as `CellSymbol.test.jsx` to assert glyph selection and color rendering in the React component.

#### 5. Update repo documentation and UI test planning
- Update `tictactoe/README.md` so it describes the project as a React app and reflects the final script/tooling setup after the migration.
- Create `.claude/commands/e2e/test_tictactoe_react_gameplay.md` using the documented scenario format, covering page load, a human move, the computer response, and a board reset via the start/reset controls.
- Confirm the plan and README language stay focused on preserving the current gameplay mechanics rather than changing game rules or visuals.

#### 6. Run the Validation Commands
- Execute every command in `### Validation Commands` and resolve any failures before considering the chore complete.

### Validation Commands
Execute every command to validate this repo's changes with zero regressions.
- `cd tictactoe && npx vitest run src/game.test.js` — targeted gameplay-logic regression suite
- `cd tictactoe && npx vitest run src/components/CellSymbol.test.jsx` — targeted symbol-rendering regression suite
- `cd tictactoe && npx vitest run src/App.test.jsx` — targeted React gameplay interaction suite
- `cd tictactoe && npx eslint src/main.jsx src/App.jsx src/components src/game.js` — lint only the React migration surface
- `cd tictactoe && npm run build` — production build regression gate for the React/Vite app
- `cd tictactoe && npm test` — full suite regression gate (last)

### Notes
- Preserve `src/game.js` behavior unless a small, well-justified API change is needed to simplify the React state flow; the chore is a framework swap, not a gameplay redesign.
- This repo is JavaScript-only today, so there is no separate TypeScript type-check command to include unless the implementation intentionally introduces TypeScript, which would broaden scope.

---

## E2E Tests
- `.claude/commands/e2e/test_tictactoe_react_gameplay.md` — Load the React app, play one human move, confirm the computer responds, then reset or swap the starting player and confirm the board state resets correctly.

Full plan for reference:

# Chore: Replace Vue with React in `tictactoe`

## Chore Description
Replace the `tictactoe` frontend from Vue to React while preserving the existing gameplay rules, turn flow, computer-move behavior, styling, and browser-facing UX. The current repo is already on Vite, Tailwind, Vitest, and a modern `src/game.js` logic module, so this chore should focus on swapping the UI framework and its test/lint/build wiring rather than reworking the game logic itself. The implementation must also update the project README and add a focused E2E scenario for the UI migration.

---

## tictactoe

### Relevant Files
- `tictactoe/package.json` — replace Vue-specific runtime and tooling with the React equivalents while preserving the existing Vite/Vitest/Tailwind workflow and script names where possible.
- `tictactoe/vite.config.js` — switch from the Vue Vite plugin to the React plugin and keep the existing dev-server/test behavior intact.
- `tictactoe/eslint.config.js` — remove Vue parsing/rules and replace them with a React-oriented flat config scoped to the new JSX files.
- `tictactoe/src/main.js` — current Vue mount point; this should become the React entry and likely move to `main.jsx`.
- `tictactoe/src/App.vue` — main gameplay UI and state orchestration; this is the largest Vue component that needs a React rewrite.
- `tictactoe/src/game.js` — pure gameplay logic already extracted from the UI; preserve this module as the behavioral contract for the React rewrite.
- `tictactoe/src/assets/main.css` — shared Tailwind/component-layer styling that should continue to drive the React UI.
- `tictactoe/src/components/CellSymbol.vue` — Vue-only rendering wrapper for board symbols; replace with a React component while keeping the same glyph and color behavior.
- `tictactoe/src/components/Icons.vue` — SVG sprite definitions currently rendered through Vue; replace with a React component or static sprite include without changing symbol ids.
- `tictactoe/src/App.test.js` — current Vue Test Utils coverage for click-to-play and reset behavior; rewrite around the React app.
- `tictactoe/src/components/CellSymbol.test.js` — current Vue component assertions for symbol rendering; rewrite for the React component.
- `tictactoe/src/game.test.js` — targeted logic regression suite that should remain in place and continue to validate gameplay rules independently of the framework swap.
- `tictactoe/README.md` — update framework references, local-development instructions, and testing notes to match the React implementation.
- `.claude/commands/e2e/README.md` — reference format for the new UI scenario file that this chore must add.

#### New Files
- `tictactoe/src/main.jsx` — React entrypoint replacing the Vue app bootstrap.
- `tictactoe/src/App.jsx` — React version of the gameplay shell.
- `tictactoe/src/components/CellSymbol.jsx` — React rendering for X/O board cells.
- `tictactoe/src/components/Icons.jsx` — React-hosted SVG symbol definitions used by `CellSymbol`.
- `tictactoe/src/App.test.jsx` — React Testing Library or equivalent component-level gameplay coverage.
- `tictactoe/src/components/CellSymbol.test.jsx` — React component rendering coverage for symbol selection and styling.
- `.claude/commands/e2e/test_tictactoe_react_gameplay.md` — focused UI scenario covering a playable round and reset flow after the React migration.

### Step by Step Tasks
IMPORTANT: Execute every step in order, top to bottom.

#### 1. Replace framework dependencies and config
- Update `tictactoe/package.json` to remove Vue runtime/test/lint packages and add `react`, `react-dom`, `@vitejs/plugin-react`, and the React-compatible test utilities needed for the existing Vitest/jsdom setup.
- Update `tictactoe/vite.config.js` to use the React Vite plugin while preserving the current `server.port` and `test` settings.
- Update `tictactoe/eslint.config.js` so the repo lints the new `*.jsx` files instead of Vue single-file components.

#### 2. Migrate the app entry and gameplay shell
- Replace `tictactoe/src/main.js` with a React entrypoint in `tictactoe/src/main.jsx` that mounts the app into `#app` and continues to import `src/assets/main.css`.
- Rewrite `tictactoe/src/App.vue` as `tictactoe/src/App.jsx`, carrying over the existing `startingPlayer`, `turn`, `gameState`, `strategyIndex`, modal messaging, reset flow, and computer-turn behavior.
- Keep `tictactoe/src/game.js` as the source of truth for outcome detection and AI move selection; only make minimal API adjustments if React integration truly requires them.

#### 3. Port the Vue-only presentation components
- Replace `tictactoe/src/components/CellSymbol.vue` with `CellSymbol.jsx`, preserving the current symbol mapping, inline color behavior, and SVG usage.
- Replace `tictactoe/src/components/Icons.vue` with `Icons.jsx` or an equivalent static React-hosted sprite so `#icon--circle` and `#icon--x` still resolve correctly.
- Remove the obsolete Vue component files once their React replacements are wired in.

#### 4. Rewrite the UI-facing tests around React
- Keep `tictactoe/src/game.test.js` in place as the framework-independent gameplay regression suite.
- Rewrite `tictactoe/src/App.test.js` as `App.test.jsx` to cover the existing user-visible behaviors: human move, computer response, and reset flow.
- Rewrite `tictactoe/src/components/CellSymbol.test.js` as `CellSymbol.test.jsx` to assert glyph selection and color rendering in the React component.

#### 5. Update repo documentation and UI test planning
- Update `tictactoe/README.md` so it describes the project as a React app and reflects the final script/tooling setup after the migration.
- Create `.claude/commands/e2e/test_tictactoe_react_gameplay.md` using the documented scenario format, covering page load, a human move, the computer response, and a board reset via the start/reset controls.
- Confirm the plan and README language stay focused on preserving the current gameplay mechanics rather than changing game rules or visuals.

#### 6. Run the Validation Commands
- Execute every command in `### Validation Commands` and resolve any failures before considering the chore complete.

### Validation Commands
Execute every command to validate this repo's changes with zero regressions.
- `cd tictactoe && npx vitest run src/game.test.js` — targeted gameplay-logic regression suite
- `cd tictactoe && npx vitest run src/components/CellSymbol.test.jsx` — targeted symbol-rendering regression suite
- `cd tictactoe && npx vitest run src/App.test.jsx` — targeted React gameplay interaction suite
- `cd tictactoe && npx eslint src/main.jsx src/App.jsx src/components src/game.js` — lint only the React migration surface
- `cd tictactoe && npm run build` — production build regression gate for the React/Vite app
- `cd tictactoe && npm test` — full suite regression gate (last)

### Notes
- Preserve `src/game.js` behavior unless a small, well-justified API change is needed to simplify the React state flow; the chore is a framework swap, not a gameplay redesign.
- This repo is JavaScript-only today, so there is no separate TypeScript type-check command to include unless the implementation intentionally introduces TypeScript, which would broaden scope.

---

## E2E Tests
- `.claude/commands/e2e/test_tictactoe_react_gameplay.md` — Load the React app, play one human move, confirm the computer responds, then reset or swap the starting player and confirm the board state resets correctly.

Multi-repo path context:
- Project/docs root: /Users/hbilles/projects/test-agents-multi
- Target repo root: /Users/hbilles/projects/test-agents-multi/tictactoe
- Keep code changes scoped to the target repo root.
- If the plan asks for top-level ADW assets such as `.claude/commands/e2e/...`, create them under the project/docs root, not under the target repo.

When finished, output:
- A concise bullet list summarizing the completed work
- A final `git diff --stat` summary